#!/bin/bash

#PBS -S /bin/bash
#PBS -l select=1:ncpus=28,walltime=24:00:00
#PBS -j oe
#PBS -N presmooth_dataset

FILENAME="main_presmooth_jb"

cd "${PBS_O_WORKDIR}"

# clean log
rm -f log_datasetpresmooth.txt compile_datasetpresmooth.txt
echo "cleaning log files..." >> log_datasetpresmooth.txt

# init spack
echo "loading spack" >> log_datasetpresmooth.txt
if [ ! -f "${HOME}/spack.sh" ]; then
    echo "spack configuration file not found" >> log_datasetpresmooth.txt
    exit 1
fi
source "${HOME}/spack.sh" >> log_datasetpresmooth.txt 2>&1

echo "loading fdapde_devel environment" >> log_datasetpresmooth.txt
spack env activate fdapde_devel >> log_datasetpresmooth.txt 2>&1

# check packages
EIGEN_LOC="$(spack location -i eigen)"
if [ -z "$EIGEN_LOC" ]; then
    echo "Eigen dependency not found" >> log_datasetpresmooth.txt
    exit 1
fi
echo "Eigen library found at ${EIGEN_LOC}" >> log_datasetpresmooth.txt

# compile
echo "compiling..." >> log_datasetpresmooth.txt
g++ -o $FILENAME $FILENAME.cpp \
    -I./fdaPDE-cpp \
    -I./fdaPDE-cpp/fdaPDE/core \
    -I"$EIGEN_LOC/include/eigen3" \
    -march=native -O2 -std=c++20 >> compile_datasetpresmooth.txt 2>&1

# run
echo "launching executable..." >> log_datasetpresmooth.txt
./$FILENAME >> log_datasetpresmooth.txt 2>&1
