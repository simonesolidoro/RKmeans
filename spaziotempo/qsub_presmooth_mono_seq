#!/bin/bash

#PBS -S /bin/bash
#PBS -l select=1:ncpus=28,walltime=24:00:00
#PBS -j oe
#PBS -N presmooth_mono_seq_vicini

FILENAME="src_vicini/main_presmooth_mono_k_seq"

cd "${PBS_O_WORKDIR}"

# clean log
rm -f log_pms.txt compile_pms.txt
echo "cleaning log files..." >> log_pms.txt

# init spack
echo "loading spack" >> log_pms.txt
if [ ! -f "${HOME}/spack.sh" ]; then
    echo "spack configuration file not found" >> log_pms.txt
    exit 1
fi
source "${HOME}/spack.sh" >> log_pms.txt 2>&1

echo "loading fdapde_devel environment" >> log_pms.txt
spack env activate fdapde_devel >> log_pms.txt 2>&1

# check packages
EIGEN_LOC="$(spack location -i eigen)"
if [ -z "$EIGEN_LOC" ]; then
    echo "Eigen dependency not found" >> log_pms.txt
    exit 1
fi
echo "Eigen library found at ${EIGEN_LOC}" >> log_pms.txt

# compile
echo "compiling..." >> log_pms.txt
g++ -o $FILENAME $FILENAME.cpp \
    -I../application/fdaPDE-cpp \
    -I../application/fdaPDE-cpp/fdaPDE/core \
    -I"$EIGEN_LOC/include/eigen3" \
    -march=native -O2 -std=c++20 >> compile_pms.txt 2>&1

# run
echo "launching executable..." >> log_pms.txt
./$FILENAME >> log_pms.txt 2>&1
